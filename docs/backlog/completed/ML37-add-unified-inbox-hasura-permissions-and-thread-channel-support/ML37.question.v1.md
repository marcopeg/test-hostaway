# Refinement Questions v1 - Add unified inbox Hasura permissions and thread channel support

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. Should `data.threads.channel_id` be mandatory?

The subscription requirement says the thread list must show `channel_id` and channel logo. Should every thread require a non-null `channel_id`, including listing-only threads, or should it be nullable for imported/legacy edge cases?

Answer:

yes

### 2. What is the initial read scope for role `user`?

For this first permissions pass, should a `user` role be allowed to read every thread, message, booking, listing, guest, channel, and pending message in their `x-hasura-tenant-id`, or should any table already be filtered by `x-hasura-user-id`?

Answer:

the real protection is by tenant-id.

the user-id is realluy only necessaru for creating a new outgoing message and presetting the operator-id there.

### 3. Who can read pending messages?

Should `data.messages_pending` be visible to every user in the tenant, or only to the operator whose `operator_id` matches `x-hasura-user-id`?

Answer:

scope by tenant-id

### 4. How should the thread-list expose the last message?

Is it acceptable for the GraphQL subscription to fetch the latest message through an ordered/limited relationship such as `messages(order_by: {sent_at: desc}, limit: 1)`, or do you want a dedicated Hasura relationship/view for `last_message`?

Answer:

go with the relationhip. it's mvp!

### 5. Should this task include write permissions?

The request mentions subscribing/reading data. Should this task stay read/subscription-only, or should it also add insert permissions for `data.messages_pending` so the Apollo client can create optimistic outgoing messages?

Answer:

only write permission is to the messages_pending where we shoudl preset the tenant and operator id by the session and - if possible - make sure that the thread/tenant is respected.
