# Plan - Add unified inbox Hasura permissions and thread channel support

## Goal

Expose the unified inbox through tenant-scoped Hasura permissions and add mandatory thread channel support so Apollo can subscribe to thread lists, single-thread details, and insert pending outgoing messages.

## Milestones

### Milestone 1 - Schema and seed channel support

- [x] Step 1 - Add mandatory thread channel column
  - Achieve: `data.threads` stores an explicit non-null `channel_id` for every thread.
  - Create: n/a
  - Modify: `apps/bff/migrations/default/20260521161000_unified_inbox_data_model/up.sql`
  - Delete: n/a
  - Touch points: Thread table definition, channel foreign key, indexes.
  - Validation: Migration applies from a clean database.
  - Notes: Added `data.threads.channel_id` as non-null, with a foreign key to `data.channels` and channel-focused indexes. The existing down migration already drops the schemas, so no down-file change was needed.

- [x] Step 2 - Update seed data
  - Achieve: Every seeded thread includes a valid channel value.
  - Create: n/a
  - Modify: `apps/bff/seeds/default/default.sql`
  - Delete: n/a
  - Touch points: Thread inserts and any thread-dependent smoke data.
  - Validation: Seeds load successfully and all thread channel IDs resolve.
  - Notes: All seeded threads now include channel IDs; booking-backed threads match their booking channel.

### Milestone 2 - Hasura metadata relationships and permissions

- [x] Step 1 - Track channel relationship on threads
  - Achieve: Hasura exposes `data_threads.channel` and keeps existing relationships needed for thread detail subscriptions.
  - Create: n/a
  - Modify: `apps/bff/metadata/databases/default/tables/data_threads.yaml`
  - Delete: n/a
  - Touch points: Object relationships and array relationships.
  - Validation: Metadata consistency check succeeds.
  - Notes: Added `data_threads.channel` and `data_channels.threads`; last-message retrieval stays on the messages relationship with ordering and limit.

- [x] Step 2 - Add tenant-scoped user select permissions
  - Achieve: Role `user` can read subscription fields for threads, channels, messages, pending messages, listings, bookings, guests, and users/operators without cross-tenant leakage.
  - Create: n/a
  - Modify: Table metadata YAML files under `apps/bff/metadata/databases/default/tables`.
  - Delete: n/a
  - Touch points: `select_permissions` blocks for `user`.
  - Validation: User-role smoke GraphQL can query thread list and single-thread details.
  - Notes: Read permissions are tenant scoped, not user scoped; global channels are readable as shared reference data.

- [x] Step 3 - Add pending-message insert permission
  - Achieve: Role `user` can insert into `data.messages_pending` with `tenant_id` and `operator_id` preset from session variables.
  - Create: n/a
  - Modify: `apps/bff/metadata/databases/default/tables/data_messages_pending.yaml`
  - Delete: n/a
  - Touch points: Insert permission, column presets, check constraint/filter expressible in Hasura metadata.
  - Validation: User-role smoke mutation inserts a pending message without trusted tenant/operator client input.
  - Notes: Insert permission presets `tenant_id` and `operator_id`; the check verifies the referenced thread belongs to the session tenant.

### Milestone 3 - Validation and notes

- [x] Step 1 - Run backend lifecycle validation
  - Achieve: The changed migration, metadata, and seeds work through the Makefile workflow.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `make down`, `make start`, `make migrate.status`, `make hasura.debug`.
  - Validation: Commands complete successfully.
  - Notes: `make down`, `make start`, `make migrate.status`, and `make hasura.debug` completed; `make migrate.status` needed elevated localhost access in the sandbox.

- [x] Step 2 - Run GraphQL smoke checks
  - Achieve: User-role headers can read thread-list data, read single-thread data, and insert a pending outgoing message.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: Hasura GraphQL endpoint at `http://localhost:8080/v1/graphql`.
  - Validation: Smoke responses include expected data and no permission errors.
  - Notes: Thread-list query, single-thread query, pending-message insert, cross-tenant read denial, and cross-tenant insert denial were verified with user-role headers.

- [x] Step 3 - Record execution results
  - Achieve: Plan checkboxes and execution notes reflect the final implementation and validation outcome.
  - Create: `docs/backlog/wip/ML37-add-unified-inbox-hasura-permissions-and-thread-channel-support/ML37.notes.md`
  - Modify: `ML37.plan.md`
  - Delete: n/a
  - Touch points: Backlog task folder.
  - Validation: Backlog indexes point at the WIP task.
  - Notes: Execution notes record the implemented permission model and validation results.
