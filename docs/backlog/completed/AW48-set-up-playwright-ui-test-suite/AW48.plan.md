# Plan - Set up Playwright UI test suite

## Goal

Install and configure a focused Playwright UI suite for the React app, runnable through `make test.ui`, with initial coverage for overview and inbox thread rendering.

## Milestones

### Milestone 1 - Configure Playwright runner

- [x] Step 1 - Add package scripts and dependency
  - Achieve: The webapp can invoke Playwright through npm.
  - Create: n/a
  - Modify: `apps/webapp/package.json`, `apps/webapp/package-lock.json`
  - Delete: n/a
  - Touch points: npm dev dependency and UI test script.
  - Validation: Package metadata contains the Playwright script and dependency.
  - Notes: Use the package lock update produced by npm.

- [x] Step 2 - Add Playwright config
  - Achieve: UI tests run from `apps/webapp/tests/e2e` with Vite started by Playwright.
  - Create: `apps/webapp/playwright.config.ts`
  - Modify: n/a
  - Delete: n/a
  - Touch points: base URL, Chromium project, webServer, traces/screenshots.
  - Validation: Playwright can discover the e2e test directory.
  - Notes: Reuse an existing server outside CI.

### Milestone 2 - Add stable UI coverage hooks and tests

- [x] Step 1 - Add narrow test hooks where roles/text are insufficient
  - Achieve: Tests can select rendered threads and verify message sender kind deterministically.
  - Create: n/a
  - Modify: inbox thread list and message rendering components as needed.
  - Delete: n/a
  - Touch points: `ThreadList`, `MessageList`
  - Validation: App still builds and existing UI behavior is unchanged.
  - Notes: Prefer visible selectors for navigation and headings.

- [x] Step 2 - Add first e2e specs
  - Achieve: Tests cover overview work-in-progress state and inbox first-thread message provenance.
  - Create: `apps/webapp/tests/e2e/navigation.spec.ts`, `apps/webapp/tests/e2e/inbox.spec.ts`
  - Modify: n/a
  - Delete: n/a
  - Touch points: `/overview`, `/inbox`, `/inbox/$threadId`
  - Validation: Playwright assertions pass against seeded data.
  - Notes: The inbox test opens the first visible thread.

### Milestone 3 - Expose Makefile target and verify

- [x] Step 1 - Add root Makefile target
  - Achieve: `make test.ui` runs the webapp Playwright suite.
  - Create: n/a
  - Modify: `Makefile`
  - Delete: n/a
  - Touch points: frontend task section.
  - Validation: `make test.ui` invokes the webapp script.
  - Notes: Backend lifecycle remains outside the target.

- [x] Step 2 - Run validation
  - Achieve: Confirm the implementation works locally.
  - Create: n/a
  - Modify: plan and execution notes.
  - Delete: n/a
  - Touch points: `npm run build`, `make test.ui`
  - Validation: Build and UI tests pass, or failures are documented with concrete blockers.
  - Notes: Chromium browser cache was installed with Playwright before the suite could run locally. `npm run build`, `npm run lint`, and `make test.ui` pass.
