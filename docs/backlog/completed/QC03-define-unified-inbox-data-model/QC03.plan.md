# Plan - Define unified inbox data model

## Goal

Replace the todo boilerplate Hasura state with a unified inbox PostgreSQL model, metadata tracking, and realistic seed data for Hostaway conversations.

## Milestones

### Milestone 1 - Replace SQL schema migration

- [x] Step 1 - Remove todo migration
  - Achieve: The todo boilerplate migration is no longer part of the active migration set.
  - Create: n/a
  - Modify: n/a
  - Delete: `apps/bff/migrations/default/20260521133000_basic_todos_app`
  - Touch points: Hasura migration folder.
  - Validation: No todo migration files remain.
  - Notes: Removed the old todo migration SQL files.
- [x] Step 2 - Add unified inbox migration
  - Achieve: Define `auth.tenants`, `auth.users`, `data.guests`, `data.channels`, `data.listings`, `data.bookings`, `data.threads`, `data.messages`, and `data.messages_pending`.
  - Create: New migration folder under `apps/bff/migrations/default`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: Migration `up.sql` and `down.sql`.
  - Validation: SQL contains requested primary keys, foreign keys, lifecycle fields, and indexes.
  - Notes: Added `20260521161000_unified_inbox_data_model` with schemas, tables, foreign keys, lifecycle fields, and indexes.

### Milestone 2 - Replace Hasura metadata

- [x] Step 1 - Remove todo metadata
  - Achieve: Todo table tracking is gone.
  - Create: n/a
  - Modify: `apps/bff/metadata/databases/default/tables/tables.yaml`
  - Delete: Todo metadata files.
  - Touch points: Hasura metadata table includes.
  - Validation: No `data_todos` metadata remains.
  - Notes: Removed todo tracking metadata from table includes and deleted `data_todos.yaml`.
- [x] Step 2 - Add unified inbox metadata
  - Achieve: All new tables are tracked with relationships based on foreign keys.
  - Create: Metadata YAML files for each table.
  - Modify: `tables.yaml`.
  - Delete: n/a
  - Touch points: Hasura metadata.
  - Validation: Metadata references existing table names and relationship targets.
  - Notes: Added metadata files for all unified inbox tables and relationships.

### Milestone 3 - Replace seed and helper SQL

- [x] Step 1 - Replace default seed
  - Achieve: Seed data covers tenants, users, guests, channels, listings, bookings, threads, messages, and pending messages.
  - Create: n/a
  - Modify: `apps/bff/seeds/default/default.sql`.
  - Delete: n/a
  - Touch points: Static seed data.
  - Validation: Seed insert order respects foreign keys and includes at least 10 threads with conversations.
  - Notes: Replaced todo seed with deterministic unified inbox sample data.
- [x] Step 2 - Remove todo helper SQL
  - Achieve: Ad hoc todo helper is no longer present.
  - Create: n/a
  - Modify: n/a
  - Delete: `apps/bff/sql/default/add-todo.sql`.
  - Touch points: Developer SQL helpers.
  - Validation: No todo helper file remains.
  - Notes: Removed `apps/bff/sql/default/add-todo.sql`.

### Milestone 4 - Verify and document execution

- [x] Step 1 - Run backend validation
  - Achieve: Confirm the changed Hasura state is structurally valid.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: Makefile backend workflow.
  - Validation: Run available make targets or report environment blockers.
  - Notes: `make down` plus `make start` applied migrations, metadata, and seeds from a clean Docker volume; `make migrate.status` reported `20260521161000 unified_inbox_data_model` present in source and database; `make hasura.debug` reported metadata is consistent; a GraphQL smoke query returned threads with tenant, listing, booking, message, guest, operator, and pending-message relationships.
- [x] Step 2 - Record execution notes
  - Achieve: Capture implementation decisions, deviations, and limitations.
  - Create: `QC03.notes.md` if missing.
  - Modify: Plan checkboxes and notes.
  - Delete: n/a
  - Touch points: Backlog task folder.
  - Validation: Task remains in WIP and plan reflects completed work.
  - Notes: Execution notes updated with validation results and known limitations.
