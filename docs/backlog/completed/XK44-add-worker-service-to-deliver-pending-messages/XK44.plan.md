# Plan - Add worker service to deliver pending messages

## Goal

Add a small Dockerized Node worker that polls `data.messages_pending`, moves pending operator replies into `data.messages`, deletes processed pending rows, and update the inbox to receive thread/message changes through subscriptions.

## Milestones

### Milestone 1 - Worker service scaffold

- [x] Step 1 - Create `apps/worker` Node project
  - Achieve: Establish the worker as a minimal Node service in the existing `apps` layout.
  - Create: `apps/worker/package.json`, source entrypoint, and runtime config.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `apps/worker`
  - Validation: Worker project installs/builds cleanly.
  - Notes: Keep TypeScript/tooling out unless needed; this is a small proof of concept.

### Milestone 2 - Pending-message delivery loop

- [x] Step 1 - Implement polling and serial processing
  - Achieve: Poll every 5 seconds, select one pending row at a time in deterministic order, insert into `data.messages`, then delete from `data.messages_pending`.
  - Create: Worker processing logic.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `apps/worker`
  - Validation: Manual DB smoke check can observe pending rows moving to persisted messages.
  - Notes: Direct Postgres access only; no retry/status/dead-letter flow.

- [x] Step 2 - Wait for Hasura health before polling
  - Achieve: Worker starts polling only after Hasura health is available.
  - Create: Health wait helper.
  - Modify: Worker entrypoint.
  - Delete: n/a
  - Touch points: `apps/worker`
  - Validation: Worker logs waiting/ready state on startup.
  - Notes: Compose also wires dependencies, but the worker should perform its own wait.

### Milestone 3 - Docker Compose integration

- [x] Step 1 - Dockerize the worker
  - Achieve: Build and run the worker from Docker Compose.
  - Create: `apps/worker/Dockerfile`.
  - Modify: `docker-compose.yml`.
  - Delete: n/a
  - Touch points: `apps/worker/Dockerfile`, `docker-compose.yml`.
  - Validation: `docker compose config` succeeds.
  - Notes: Use the same local Postgres connection style as Hasura.

### Milestone 4 - Inbox subscriptions

- [x] Step 1 - Replace inbox query/refetch flow with subscriptions
  - Achieve: Thread list, message list, and aggregate stats update live through GraphQL subscriptions.
  - Create: n/a
  - Modify: `apps/webapp/src/views/inbox/InboxView.tsx`.
  - Delete: Refetch dependency after sending replies.
  - Touch points: Inbox data container.
  - Validation: Webapp build/lint passes.
  - Notes: Keep the existing mutation for sending pending replies.

### Milestone 5 - Verification and notes

- [x] Step 1 - Run available static checks and compose validation
  - Achieve: Catch TypeScript, lint, package, and compose errors.
  - Create: n/a
  - Modify: Execution notes with results.
  - Delete: n/a
  - Touch points: `apps/webapp`, `docker-compose.yml`, `apps/worker`.
  - Validation: `npm run build`, `npm run lint`, `docker compose config`, and worker package checks as available.
  - Notes: Manual smoke can be documented if local backend is unavailable.

- [x] Step 2 - Attempt manual smoke when backend is available
  - Achieve: Insert or submit a pending reply, wait at least 6 seconds, confirm it moved from pending to persisted.
  - Create: n/a
  - Modify: Execution notes with result or blocker.
  - Delete: n/a
  - Touch points: Local Hasura/Postgres stack.
  - Validation: Pending row disappears and persisted message appears.
  - Notes: This test is intentionally not automated for this task.
