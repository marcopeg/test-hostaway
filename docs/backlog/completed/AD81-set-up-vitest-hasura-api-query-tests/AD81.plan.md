# Plan - Set up Vitest Hasura API query tests

## Goal

Add the minimal Vitest-based Hasura API query test suite for tenant-scoped thread/message reads, runnable through `make test.api`.

## Milestones

### Milestone 1 - Wire the Test Runner

- [x] Step 1 - Add Vitest package support
  - Achieve: The webapp package can run API tests with Vitest.
  - Create: n/a
  - Modify: `apps/webapp/package.json`, `apps/webapp/package-lock.json`
  - Delete: n/a
  - Touch points: npm dev dependencies and scripts.
  - Validation: `npm run test:api -- --help` or equivalent command resolution succeeds.
  - Notes: Keep this Vitest-only; do not add Playwright.

- [x] Step 2 - Add root Makefile entry point
  - Achieve: `make test.api` delegates to the webapp API suite.
  - Create: n/a
  - Modify: `Makefile`
  - Delete: n/a
  - Touch points: Webapp Tasks section.
  - Validation: `make test.api` invokes the webapp script.
  - Notes: The target assumes the backend is already running.

### Milestone 2 - Add Hasura Query Test Utilities

- [x] Step 1 - Add typed GraphQL helper
  - Achieve: Tests can call Hasura with application role headers and configurable endpoint/admin secret.
  - Create: `apps/webapp/tests/hasura/graphql.ts`
  - Modify: n/a
  - Delete: n/a
  - Touch points: Hasura GraphQL HTTP endpoint.
  - Validation: Helper compiles under Vitest/TypeScript.
  - Notes: Defaults should match `TESTING.md`.

- [x] Step 2 - Add deterministic state reset helper
  - Achieve: The suite can run `make migrate.rebuild && make seed` between tests.
  - Create: Test-local helper or function near the suite.
  - Modify: n/a
  - Delete: n/a
  - Touch points: child process execution from `apps/webapp` back to repo root.
  - Validation: Reset command runs before the test suite assertions.
  - Notes: Keep it serial and explicit.

### Milestone 3 - Add Tenant Query Coverage

- [x] Step 1 - Add the thread/message tenancy test
  - Achieve: One test queries `gre`, `blu`, and an unknown tenant through Hasura `user` role headers.
  - Create: `apps/webapp/tests/hasura/tenancy.test.ts`
  - Modify: n/a
  - Delete: n/a
  - Touch points: `data_threads` with nested persisted `messages`.
  - Validation: Assertions verify tenant scoping, non-empty messages per returned thread, different message sets for `gre` and `blu`, and empty results for unknown tenant.
  - Notes: Do not include `pending_messages` or mutations.

- [x] Step 2 - Run focused validation
  - Achieve: The new API suite passes against the local backend when available.
  - Create: n/a
  - Modify: Plan and notes progress.
  - Delete: n/a
  - Touch points: `make test.api`
  - Validation: Run `make test.api`; if backend/dependencies block execution, record the specific failure.
  - Notes: `make test.api` passed against the already-running local backend. `npm run lint` also passed for the webapp package.
