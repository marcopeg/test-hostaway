# Refinement Questions v1 — Create Hostaway UI moodboard signature

Answer inline under each question, then return to chat and say `next round`.

## Context Observed

The task folder includes these screenshots:

- `home.png`: Hostaway marketing homepage with brand palette, headline scale, navigation, and product mockup.
- `messaging.png`: guest messaging workspace with inbox list, conversation area, AI suggested reply card, and reservation detail sidebar.
- `reservations.png`: app dashboard/reservations overview with left navigation, metric cards, and reservation lists.
- `calendar.png`: dense multi-listing reservation calendar with filters, tabs, rows, booking bars, and channel icons.
- `card.png`: compact reservation card with guest/channel identity, booking metadata, price, and payment status.

## Questions

### 1. Where should the final `ui-signature` artifact live?

For example: only inside this task folder, under a shared docs path such as `docs/ui-signature.md`, or both.

Answer:

.agents/skills/hostaway-ui

Make sure the description will make the agent pick it up

### 2. Which screenshots are canonical for the app UI signature?

Should the signature be based mainly on the operational app screenshots (`messaging.png`, `reservations.png`, `calendar.png`, `card.png`), or should the marketing homepage (`home.png`) also influence app typography, color, and brand treatment?

Answer:

Don't consider home. mainly about the messaging and use the others as side information

### 3. What should the `ui-signature` include?

Confirm whether it should cover only visual guidance, or also reusable implementation guidance such as design tokens, layout rules, component patterns, icon usage, responsive behavior, and state treatment.

Answer:

Anything relevant for the agent to use Tailwind in a way that produces a recognisable Hostaway feeling when using the app.

### 4. How close should the implementation guidance stay to Hostaway?

Should future UI work aim for a close Hostaway-like match, or use the screenshots as inspiration while adapting freely to this app's own structure?

Answer:

As close as possible, without becoming a bottleneck in the fast prototyping.

### 5. Which first product surface should the signature optimize for?

Choose the primary target workflow: unified inbox, reservations dashboard, calendar, reservation detail cards, or a broader admin shell.

Answer:

messaging

### 6. Should the signature define concrete frontend tokens?

Examples: primary orange/teal colors, muted gray palette, sidebar width, table/list density, border radius, typography scale, spacing increments, status badge styles, and card shadow/border rules.

Answer:

you can modify directly the apps/webapp/src/index.css setting the correct tokens for tailwind.

### 7. Are there any screenshot details that should explicitly not be copied?

Examples: exact logo treatment, marketing-page hero style, specific channel icons, exact colors, rounded booking bars, AI reply card styling, or sidebar navigation structure.

Answer:

you should not copy anything, just extract the color scheme, approximate font, spacing rules, titles subtitles, styles to render the elements in the page.

Using this skiull, the agent should be able to direct tailwind.
