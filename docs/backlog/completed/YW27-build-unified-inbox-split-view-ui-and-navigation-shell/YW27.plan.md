# Plan - Build unified inbox split-view UI and navigation shell

## Goal

Replace the todo demo with a routed unified inbox shell that provides a Hostaway-style split view, real inbox data, dashboard awareness, thread details, and pending-message aware reply state.

## Milestones

### Milestone 1 - Routing and application shell

- [x] Step 1 - Register routes and work-in-progress pages
  - Achieve: `/` redirects to `/inbox`, `/inbox` and `/inbox/$threadId` render the inbox, and every accepted main-menu path renders a shared Work in progress page.
  - Create: `apps/webapp/src/views/work-in-progress`.
  - Modify: `apps/webapp/src/App.tsx`.
  - Delete: Routed todo-list and todo-details usage from the app route tree.
  - Touch points: TanStack Router route definitions.
  - Validation: TypeScript build catches route/param mistakes.
  - Notes: Keep obsolete todo files only if unrelated exports still need them; remove routed surface first.

- [x] Step 2 - Build functional BaseLayout navigation
  - Achieve: Main menu uses router links with active state, top menu remains compact and operational, and app chrome matches the Hostaway-like UI.
  - Create: n/a.
  - Modify: `apps/webapp/src/components/layout/BaseLayout.tsx`.
  - Delete: Hard-coded `/` links.
  - Touch points: `UserInfo`, menu labels, top actions.
  - Validation: Browser check for active nav and layout fit.
  - Notes: Use existing design tokens and compact density.

### Milestone 2 - Split layout and inbox data model in the frontend

- [x] Step 1 - Add reusable SplitLayout
  - Achieve: Left, center, and optional right columns render with independent scrolling, shared side backgrounds, and a raised center content panel with top margin and top rounded corners.
  - Create: `apps/webapp/src/components/layout/split-layout/SplitLayout.tsx`, `apps/webapp/src/components/layout/split-layout/index.ts`.
  - Modify: `apps/webapp/src/components/index.ts` if needed.
  - Delete: n/a.
  - Touch points: layout sizing and responsive behavior.
  - Validation: Browser viewport check and build.
  - Notes: Avoid nested cards and preserve operational density.

- [x] Step 2 - Add inbox view containers and typed data shaping
  - Achieve: Inbox view loads threads, messages, pending messages, listing, booking, and channel data from Hasura and shapes it for the UI.
  - Create: `apps/webapp/src/views/inbox`, `views/inbox/containers/thread-list`, `views/inbox/containers/stats`, and supporting inbox containers/components as needed.
  - Modify: Apollo query usage near containers.
  - Delete: n/a.
  - Touch points: `@apollo/client`, GraphQL session role behavior, route params.
  - Validation: Build and runtime data/load/error states.
  - Notes: Merge persisted `messages` with `messages_pending` before calculating latest logical message and unanswered state.

### Milestone 3 - Inbox experience and situational awareness

- [x] Step 1 - Implement thread list and dashboard
  - Achieve: Thread list shows channel icon, guest/listing context, preview, mood/topic/stage labels, and bold guest-latest rows. `/inbox` center shows stats for total messages, unanswered, mood, booking stage, topic, and any useful extra dashboard metrics available from the data.
  - Create: reusable mood/stage/topic label components under the inbox view or shared component scope if broadly useful.
  - Modify: inbox view UI.
  - Delete: todo demo dependency from the inbox experience.
  - Touch points: dashboard aggregations, Hasura aggregate permissions if required.
  - Validation: GraphQL query runs under user role; limitations go into notes.
  - Notes: Prefer real aggregate queries; narrowly adjust metadata only when needed.

- [x] Step 2 - Implement selected-thread conversation
  - Achieve: `/inbox/$threadId` renders merged message timeline, professional guest/operator bubbles, sender identity, short time labels with full timestamp on hover, and sticky expanding reply composer that inserts `messages_pending`.
  - Create: message list, message bubble, reply composer components.
  - Modify: inbox selected-thread container.
  - Delete: n/a.
  - Touch points: pending message mutation and optimistic/refresh behavior.
  - Validation: Build, browser interaction, and mutation smoke if local Hasura is available.
  - Notes: Pending outgoing messages should influence unanswered state immediately.

- [x] Step 3 - Implement details panel
  - Achieve: Right panel shows mood/topic indicators, listing-first details, mocked listing image carousel, proposed listing fields, booking timing, from-to visualization, and booking stage.
  - Create: inbox details panel components.
  - Modify: inbox selected-thread layout.
  - Delete: n/a.
  - Touch points: listing and booking data.
  - Validation: Browser check for panel scrolling and no overlap.
  - Notes: Mock only missing listing images/proposed fields; keep real listing and booking data where available.

### Milestone 4 - Cleanup, validation, and task notes

- [x] Step 1 - Remove todo demo surface and unused exports
  - Achieve: Todo demo containers are no longer routed or exported if unused.
  - Create: n/a.
  - Modify: `apps/webapp/src/containers/index.ts` and related imports.
  - Delete: obsolete todo files if no longer referenced.
  - Touch points: route imports, container barrel exports.
  - Validation: `rg` for todo route/demo references and TypeScript build.
  - Notes: Avoid unrelated cleanup.

- [x] Step 2 - Verify and document limitations
  - Achieve: Build/lint or equivalent checks run, local browser verification is performed, and implementation limitations are recorded in notes.
  - Create: `YW27.notes.md` if not already present.
  - Modify: `YW27.plan.md` progress and notes.
  - Delete: n/a.
  - Touch points: `npm run build`, browser, Hasura metadata if touched.
  - Validation: Report command results and any blocked checks.
  - Notes: Do not complete/archive the task from execution skill.
