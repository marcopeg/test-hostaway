# Plan — Create Hostaway UI moodboard signature

## Goal

Create a reusable Hostaway-like UI skill and Tailwind theme foundation so future React/Tailwind app work can quickly reproduce the operational messaging-oriented interface style captured in the screenshots.

## Milestones

### Milestone 1 — Skill Scaffold

- [x] Step 1 — Initialize the local skill
  - Achieve: Create `.agents/skills/hostaway-ui` using the standard skill scaffold.
  - Create: `.agents/skills/hostaway-ui/SKILL.md`, `.agents/skills/hostaway-ui/agents/openai.yaml`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `.agents/skills/hostaway-ui`.
  - Validation: Skill folder exists and passes quick validation.
  - Notes: The description must trigger on Hostaway-like operational UI, unified inbox, messaging, reservations, calendar, and admin shell work.

### Milestone 2 — UI Signature Guidance

- [x] Step 2 — Write screenshot-derived guidance
  - Achieve: Replace scaffold placeholders with concise, actionable guidance for agents.
  - Create: n/a
  - Modify: `.agents/skills/hostaway-ui/SKILL.md`.
  - Delete: n/a
  - Touch points: `messaging.png`, `reservations.png`, `calendar.png`, `card.png`, `home.png`.
  - Validation: The skill states that `messaging.png` is canonical, supporting screenshots inform secondary patterns, and `home.png` must not drive app UI.
  - Notes: Guidance should describe color, typography, spacing, layout density, panels, lists, cards, badges, buttons, AI reply surfaces, and Tailwind usage without copying proprietary assets.

### Milestone 3 — Tailwind Theme Tokens

- [x] Step 3 — Add Hostaway-like Tailwind v4 tokens
  - Achieve: Define reusable tokens/base styles for the Hostaway-like operational UI signature.
  - Create: n/a
  - Modify: `apps/webapp/src/index.css`.
  - Delete: n/a
  - Touch points: Tailwind v4 `@theme`, base layer CSS variables, body defaults.
  - Validation: CSS remains valid for Tailwind v4 and preserves the existing import.
  - Notes: Tokens should support orange/teal accents, cool muted grays, compact surfaces, subtle borders, small radius, and readable operational typography.

### Milestone 4 — Verification

- [x] Step 4 — Validate and build
  - Achieve: Confirm the skill metadata and frontend CSS are usable.
  - Create: `OG27.notes.md` if missing.
  - Modify: `OG27.plan.md`, `OG27.notes.md`.
  - Delete: n/a
  - Touch points: skill validation script, webapp build command.
  - Validation: Run skill quick validation and `npm run build` for `apps/webapp` if dependencies are available.
  - Notes: Skill validation passed and `npm run build` completed successfully for `apps/webapp`.
