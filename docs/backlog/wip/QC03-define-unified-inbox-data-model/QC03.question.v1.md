# Refinement Questions v1 - Define unified inbox data model

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. Should `data.guests` be a first-class table?

`data.threads` references `guest_id`, but the draft does not yet define `data.guests`. Should we add it now? If yes, list the minimum fields and seed volume. A likely starting point is `id`, `tenant_id`, `name`, `icon`, and optional channel/customer identifiers.

Answer:

Yes I forgot, add it in the "data.guests" following similar principles as for the users. name and icon.

### 2. How should channel-specific listing identifiers be stored?

The draft describes `data.listings.channels_ids[{}]` as JSON items with `channel_id` and `listing_id`. Should this stay as a `jsonb` column on `data.listings`, or should we model it relationally as a separate table such as `data.listing_channels` with one row per channel mapping?

Answer:

Remove the field alltogether. we don't need it in the unified inbox.

### 3. What is the exact relationship between bookings and channels?

`data.bookings` includes `channel_booking_id` and `channel_listing_id`, but no direct `channel_id`. Should each booking belong to exactly one `data.channels` row? If yes, should `channel_booking_id` and `channel_listing_id` be plain external string identifiers?

Answer:

Simplify. just add "channel_id", one booking references one channel. and one internal "listing_id".

remove channel_booking_id and channel_listing_id.

### 4. What table/enum shape should we use for stages, moods, and topics?

Should `booking.stage`, `threads.mood_value`, and `threads.topic_value` be Postgres enums, check-constrained text fields, lookup tables, or plain text for speed in this exercise?

Answer:

So far just plain text.
Document the values in the task's notes.

### 5. What should define inbox ordering and unread/unhandled state?

For the UI and subscriptions, do we need denormalized fields on `data.threads`, such as `last_message_at`, `last_message_preview`, `last_message_direction`, `is_unhandled`, `assigned_user_id`, or `status`? Or should those be derived from `data.messages` for now?

Answer:

For this first iteratin we will try to use message data in join.
It's heavier, but it's a POC.

### 6. How should incoming guest messages be represented?

`data.messages.operator_id` being nullable indicates operator vs guest, but there is no `guest_id` or `direction`. Should guest messages derive the guest from `data.threads.guest_id`, or should each message explicitly store `guest_id` and/or `direction`?

Answer:

add also guest_id. i thoughtt the guest should be implied by the thread, but it could be that the booking is shared with multiple guests as well.

remove the guest_id from the thread then.

### 7. What lifecycle fields are required on each table?

The draft says to add `created_at` and `updated_at` where it makes sense. Should every domain table include both? Should message rows use immutable `created_at` plus `sent_at`, while pending messages also include outbox fields like `status`, `attempts`, `last_error`, and `processed_at`?

Answer:

don't overdo the outbox. it's just a draft for it.
I'd say to only keep a "updated_at" (no created at) - everu time that the upstream service will change the data, this is sot for that event.

### 8. What should happen to boilerplate metadata and seeds in this task?

The draft explicitly removes the todo migration. Should this same task also replace the Hasura metadata table tracking files and default seed file, or should it only create SQL migrations/seeds and leave metadata updates for a later task?

Answer:

delete without looking back.
