# Hasura BFF + React App Boilerplate

This project ships a ready-to-go webapp+bff with a GraphQL API served by Hasura and a frontend webapp based on React.

## Projects API

Most of the development lifecycle activities can be performed via `make action-name` interface.  
Read the [Makefile](./Makefile) for a complete understanding of the capabilities.
